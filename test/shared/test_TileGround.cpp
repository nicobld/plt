#include <boost/test/unit_test.hpp>

#include "../../src/shared/state/TileGround.h"

using namespace ::state;

BOOST_AUTO_TEST_CASE(TestInstanciationTileGround)
{
    //TileGroundType tileGroundTypeEx = new TileGroundType("some_path");
    //TileGround * tileGroundMock = new TileGround(tileGroundTypeEx);

    BOOST_CHECK(1);
}